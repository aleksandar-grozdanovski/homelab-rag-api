name: CI

on:
  push:
    branches: [ master, test ]
  pull_request:
    branches: [ master, test ]

permissions:
  contents: read
  packages: write

jobs:
  build-api:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore ./HomelabRAG.API

      - name: Clean
        run: dotnet clean ./HomelabRAG.API

      - name: Build
        run: dotnet build ./HomelabRAG.API -c Release --no-restore

      - name: Test
        run: |
          if [ -d "./HomelabRAG.API.Tests" ]; then
            dotnet test ./HomelabRAG.API.Tests --no-build --verbosity normal || true
          else
            echo "No tests found"
          fi

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Determine image tag
        id: tag
        run: |
          if [[ "${{ github.ref }}" == "refs/heads/test" ]]; then
            echo "tag=test" >> $GITHUB_OUTPUT
          else
            echo "tag=latest" >> $GITHUB_OUTPUT
          fi

      - name: Build and push API image
        uses: docker/build-push-action@v4
        with:
          context: ./HomelabRAG.API
          file: ./HomelabRAG.API/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/homelab-rag-api:${{ steps.tag.outputs.tag }}
            ghcr.io/${{ github.repository_owner }}/homelab-rag-api:${{ github.sha }}
          platforms: linux/amd64

  build-frontend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Determine image tag
        id: tag
        run: |
          if [[ "${{ github.ref }}" == "refs/heads/test" ]]; then
            echo "tag=test" >> $GITHUB_OUTPUT
          else
            echo "tag=latest" >> $GITHUB_OUTPUT
          fi

      - name: Build and push Frontend image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile.frontend
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/homelab-rag-frontend:${{ steps.tag.outputs.tag }}
            ghcr.io/${{ github.repository_owner }}/homelab-rag-frontend:${{ github.sha }}
          platforms: linux/amd64
